# Claude tool-use layer (PLAN.md M5–M6): ask.py (read tools), propose.py (single
# gated propose tool), prompts.py, schemas.py. Two separated tool sets; there is no
# tool that mutates the schedule directly. Follow the claude-api skill when building.
