# Gated propose layer (PLAN.md M6): NL intent -> scheduler request -> re-solve ->
# impact preview -> human Approve/Reject. The ONLY write path. Nothing persists
# without approval; no direct-mutation path exists.
